class test_arreglos{

int A[5] ;
boolean C[10];

int pruArreglos(int a, int b ){
         boolean c ;
         c =  false && C[2];
         C[a + b] = ((a < b)) ; 
         C[c] = c; //Error el indice de un arreglo tiene q ser entero. 

	return A[1]; 

}

int main(){
     
     A[1.3 + 8.3] =(-6) ; //Error el indice de un arreglo tiene q ser entero. 
     externinvk("printf", int ,"%d resultado : ", pruArreglos(4, 8 ) , A);     
    return 1;
    
}

}
