class test_arreglos{

int A[-5] ;// error la declaracion de un arreglo debe tener un entero mayor a CERO
boolean C[0];// error la declaracion de un arreglo debe tener un entero mayor a CERO

int pruArrreglos(int a, int b ){
         boolean c ;
         c =  false && C[2];
         C[a + b] = ((a < b)) ; 
         C[0] = c; 
	return A[1]; 

}

int main(){
     
     A[0] =(-6) ; 
     externinvk("printf", int ,"%d resultado : ", pruArreglos(4, 8 ) , A);     
    return 1;
    
}

}
